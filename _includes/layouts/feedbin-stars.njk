---
layout: layouts/page.njk
section: feedbin-stars
---
<p>
 These are the {{ feedbinLikes | length }} articles that I've starred on <a href="https://feedbin.com">Feedbin, my RSS reader</a>.
</p>

<div class="reading_list">

    {% for item in feedbinLikes %}
        <section {% if loop.first == false %}class="reading_list_item"{% endif %}>

            <a href="{{ item.url }}" class="reading_item_title word-wrap-break-word">
                {{ item.title }}
            </a>
            <p>{{ item.publication }}</p>
        </section>
    {% endfor %}

</div>