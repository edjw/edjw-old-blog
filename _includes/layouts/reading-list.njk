---
layout: layouts/page.njk
section: reading-list
---
<p>See what I've saved in Pocket, Raindrop, and Feedbin.</p>
<h3 class="readingListTitle">
    Latest 3 Pocket articles
</h3>

{% for i in range(0, 3) %}

    <section {% if loop.first == false %}class="reading_list_item"{% endif %}>

        <a href="{{ readingList[i].resolved_url }}"
            class="word-wrap-break-word reading_item_title">{{ readingList[i].resolved_title if readingList[i].resolved_title else readingList[i].resolved_url }}</a>
        {% if readingList
        [i].authors %}
        by {% for authorID, authorDetails in readingList
            [i].authors %}
                <em>{{ authorDetails.name | title }}
                </em>
            {% endfor %}
        {% endif %}

    </section>
{% endfor %}
<a href="/pocket-articles" class="see_all">See all Pocket articles</a>

<h3 class="readingListTitle">
    Latest 3 Raindrop links
</h3>

{% for i in range(0, 3) %}
    <section {% if loop.first == false %}class="reading_list_item"{% endif %}>

        <a href="{{ raindropLinks[i].url }}" class="word-wrap-break-word reading_item_title">
            {{ raindropLinks[i].title }}
        </a>

        <p>{{ raindropLinks[i].excerpt | truncate(150)}}</p>

    </section>
{% endfor %}
<a href="/raindrop-links" class="see_all">See all Raindrop links</a>

<h3 class="readingListTitle">
    Latest 3 Feedbin stars
</h3>

{% for i in range(0, 3) %}
    <section {% if loop.first == false %}class="reading_list_item"{% endif %}>
        <a href="{{ feedbinLikes[i].url }}" class="reading_item_title word-wrap-break-word">
            {{ feedbinLikes[i].title }}
        </a>
        {# <p>{{ feedbinLikes[i].publication }}</p> #}
    </section>
{% endfor %}
<a href="/feedbin-stars" class="see_all">See all Feedbin stars</a>