---
layout: layouts/page.njk
section: rss-likes
---
<p>
 These are the {{ raindropLinks | length }} links that I've saved on <a href="https://raindrop.io">Raindrop, my bookmarking service</a>.
</p>

<div class="reading_list">

    {% for item in raindropLinks %}
        <section {% if loop.first ==false %}class="reading_list_item"{% endif %}>

            <a href="{{ item.url }}" class="reading_item_title word-wrap-break-word">
                {{ item.title }}
            </a>
            <p>{{ item.excerpt }}</p>
        </section>
    {% endfor %}

</div>