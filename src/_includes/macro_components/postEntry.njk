{% macro postEntry(date, url, title) %}
    <section class="p-8 my-10 border-t-8 border-yellow-200 shadow-lg">
        <p class="m-0 text-base">{{ date }}</p>
        <h3 class="mt-1">
            <a href="{{ url }}" title= "{{ title }}">{{ title }}</a>
        </h3>
    </section>
{% endmacro %}